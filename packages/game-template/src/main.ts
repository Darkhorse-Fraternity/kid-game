import kaplay from "kaplay";
import "kaplay/global";

// 初始化游戏
kaplay({
  width: 800,
  height: 600,
  background: [135, 206, 235], // 天蓝色背景
});

// ==========================================
// 🎮 在这里开始你的游戏！
// 告诉 AI 你想做什么，让它帮你写代码
// ==========================================

add([
  text("🎮 开始你的游戏创作！\n\n按空格键体验震动效果", { size: 28, align: "center" }),
  pos(center()),
  anchor("center"),
]);

// 示例：按空格键让屏幕震动
onKeyPress("space", () => {
  shake(10);
});

// 示例：点击鼠标创建一个圆
onClick(() => {
  add([
    circle(20),
    pos(mousePos()),
    color(rand(0, 255), rand(0, 255), rand(0, 255)),
    scale(1),
    anchor("center"),
  ]);
});
